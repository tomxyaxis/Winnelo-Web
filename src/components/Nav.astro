<header class="nav">
  <div class="nav-container">
    <a href="/" class="nav-logo" aria-label="Winnelo home">
      <img src="/logo.svg" alt="" class="logo-mark" aria-hidden="true" />
      <span class="logo-text">Winnelo</span>
    </a>
    <button type="button" class="nav-cta" id="nav-waitlist-btn">Join Waitlist</button>
  </div>
</header>

<style>
  .nav {
    position: sticky;
    top: 0;
    z-index: 100;
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    background: rgba(255, 255, 255, 0.8);
    width: 100%;
    flex-shrink: 0;
  }

  .nav-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    max-width: 1536px;
    margin: 0 auto;
    padding: 20px 48px;
  }

  .nav-logo {
    font-family: 'Inter', sans-serif;
    font-weight: 500;
    font-style: normal;
    font-size: 20px;
    letter-spacing: -1px;
    line-height: 32px;
    display: inline-flex;
    align-items: center;
    gap: 10px;
  }

  .logo-mark {
    height: 28px;
    width: auto;
    display: block;
    flex-shrink: 0;
  }

  .logo-text { letter-spacing: -1px; position: relative; top: 1px; }

  .nav-cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #0047ab;
    color: #fff;
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 12px;
    letter-spacing: 1.2px;
    text-transform: uppercase;
    padding: 8px 24px;
    border-radius: 4px;
    border: none;
    cursor: pointer;
    transition: background 0.15s ease;
  }

  .nav-cta:hover { background: #003a8c; }

  @media (max-width: 640px) {
    .nav-container { padding: 20px 24px; }
  }
</style>
